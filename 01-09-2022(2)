#include <stdio.h>
int fact(int);
void main()
{
	int num,factorial;
  	printf("Enter a num\n");
  	scanf("%d",&num);
  	factorial=fact(num);
    printf("%d",factorial);
}
int fact(int n)
{
    int i,f=1;
    for(i=1;i<=n;i++)
    {
        f=f*i;
    }
    return f;
}
