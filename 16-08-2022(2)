#include<stdio.h>
int main()
{
	int a[10],i,j,f,n;
	printf("Enter values");
	for(i=0;i<=5;i++)
	scanf("%d",&a[i]);
		for(i=0;i<=5;i++)
		{
			n=a[i];
			f=0;
			for(j=2;j<n;j++)
			{
				if(n%j==0)
				{
					f=1;
					break;
				}
			}
			if(f==0)
			printf("\n%d is prime",n);
		}
		return 0;
	
}
